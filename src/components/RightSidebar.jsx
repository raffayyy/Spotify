import React from 'react'

function RightSidebar(props) {
  return (
    <div>
      <div>
        <h1>{props.songTitle}</h1>
        <div>
          <button></button>
          <button></button>
        </div>
      </div>
    </div>
  )
}

export default RightSidebar